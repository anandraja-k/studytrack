# Copy this file to `.env` for local overrides. `.env` itself is gitignored.
#
# Which AI backend the assistant module uses.
#   mock -> fully offline, deterministic, no API key and no network call.
#           This is the default and the only mode implemented in this submission.
# No API key variable is defined here because no real provider is called.
AI_MODE=mock
